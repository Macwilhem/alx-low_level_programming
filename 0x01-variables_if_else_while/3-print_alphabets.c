#include <stdio.h>

/*
 * main-program entry point.
 * Return:  0 no error, non zero if error
 */
int main(void)
{
	char i;

	char j;

	i = 'a';
	j = 'A';
	while
		(i <= 'z')
		{
			putchar(c);
			c++;
		}
	while
		(j <= 'Z')
		{
			putchar(j);
			j++;
		}
	putchar('\n');
	return (0);
}
